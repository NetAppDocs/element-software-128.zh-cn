---
permalink: api/reference_element_api_setsnmpacl.html 
sidebar: sidebar 
keywords: setsnmpACL,set,setting,snmp,acl,permissions,access,control,list 
summary: 您可以使用 SetSnmpACL 方法配置集群节点上的 SNMP 访问权限。使用此接口设置的值适用于集群中的所有节点，并且传递的值将替换之前对 SetSnmpACL 的任何调用中设置的所有值。另请注意，使用此接口设置的值将替换使用 SetSnmpInfo 设置的所有网络或 usmUsers 值。 
---
= 设置 SNMP 访问控制列表
:allow-uri-read: 
:icons: font
:imagesdir: ../media/


[role="lead"]
你可以使用 `SetSnmpACL`在集群节点上配置SNMP访问权限的方法。您通过此接口设置的值将应用于集群中的所有节点，并且传递的值将替换之前任何调用中设置的所有值。 `SetSnmpACL` 。另请注意，通过此接口设置的值将替换所有通过该接口设置的网络或 usmUsers 值。 `SetSnmpInfo`方法。



== 参数

该方法有以下输入参数：

|===
| 名称 | 描述 | 类型 | 默认值 | 必填项 


 a| 
网络
 a| 
列出各个网络及其对集群节点上运行的 SNMP 服务器的访问权限类型。有关可能的网络值，请参阅 SNMP 网络对象。如果禁用 SNMP v3，则此参数为必填项。
 a| 
xref:reference_element_api_network_snmp.adoc[network]
 a| 
无
 a| 
否



 a| 
usmUsers
 a| 
列出集群节点上运行的 SNMP 服务器的用户及其访问权限类型。如果启用了 SNMP v3，则此参数为必填项。
 a| 
xref:reference_element_api_usmuser.adoc[usmUser]
 a| 
无
 a| 
否

|===


== 返回值

此方法没有返回值。



== 请求示例

该方法的请求类似于以下示例：

[listing]
----
{
  "method": "SetSnmpACL",
  "params": {
    "usmUsers"  : [
      {
         "name": "jdoe",
         "access": "rouser",
         "secLevel": "priv",
         "password": "mypassword",
         "passphrase": "mypassphrase",
      }
    ]
  },
  "id" : 1
}
----


== 响应示例

此方法返回类似于以下示例的响应：

[listing]
----
{
  "id" : 1,
  "result" : {}
}
----


== 自版本以来的新版本

9.6



== 查找更多信息

xref:reference_element_api_setsnmpinfo.adoc[设置 SNMP 信息]

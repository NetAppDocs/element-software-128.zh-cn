---
permalink: api/reference_element_api_addidpclusteradmin.html 
sidebar: sidebar 
keywords: IdP,idp,cluster,admin,add,adding,addIdpclusteradmin 
summary: '您可以使用 AddIpdClusterAdmin 方法添加由第三方身份提供商 (IdP) 进行身份验证的集群管理员用户。 IdP 集群管理员帐户是根据 IdP 与用户关联的 SAML 断言中提供的 SAML 属性值信息进行配置的。如果用户成功通过身份提供商 (IdP) 的身份验证，并且 SAML 断言中的 SAML 属性语句与多个 IdP 集群管理员帐户匹配，则该用户将拥有与这些匹配的 IdP 集群管理员帐户的合并访问级别。' 
---
= 添加IDP集群管理员
:allow-uri-read: 
:icons: font
:imagesdir: ../media/


[role="lead"]
你可以使用 `AddIpdClusterAdmin`添加由第三方身份提供商 (IdP) 进行身份验证的集群管理员用户的方法。 IdP 集群管理员帐户是根据 IdP 与用户关联的 SAML 断言中提供的 SAML 属性值信息进行配置的。如果用户成功通过身份提供商 (IdP) 的身份验证，并且 SAML 断言中的 SAML 属性语句与多个 IdP 集群管理员帐户匹配，则该用户将拥有与这些匹配的 IdP 集群管理员帐户的合并访问级别。



== 参数

该方法有以下输入参数：

|===
| 名称 | 描述 | 类型 | 默认值 | 必填项 


 a| 
access
 a| 
控制此身份提供商集群管理员可以使用的方法。
 a| 
字符串数组
 a| 
无
 a| 
是



 a| 
接受最终用户许可协议
 a| 
接受最终用户许可协议。设置为 true 可向系统中添加集群管理员帐户。如果省略或设置为 false，则方法调用失败。
 a| 
布尔值
 a| 
无
 a| 
是



 a| 
属性
 a| 
JSON 对象格式的名称-值对列表。
 a| 
JSON 对象
 a| 
无
 a| 
否



 a| 
用户名
 a| 
将 SAML 属性值映射到 IdP 集群管理员（例如，email=test@example.com）。这可以通过使用特定的 SAML 主题来定义。 `NameID`或者作为 SAML 属性语句中的一个条目，例如 `eduPersonAffiliation`。
 a| 
string
 a| 
无
 a| 
是

|===


== 返回值

该方法返回以下值：

|===


| 名称 | 描述 | 类型 


 a| 
集群管理员ID
 a| 
新创建的集群管理员的唯一标识符。
 a| 
整数

|===


== 请求示例

该方法的请求类似于以下示例：

[listing]
----
{
    "method": "AddIdpClusterAdmin",
    "params": {
        "username": "email=test@example.com",
        "acceptEula": true,
        "access": ["administrator"]
    }
}
----


== 响应示例

此方法返回类似于以下示例的响应：

[listing]
----
{
    "result": {
        "clusterAdminID": 13
    }
}
----


== 自版本以来的新版本

12.0
